%p#notice= notice

.row
  .col-md-3
  .col-sm-12.col-md-6
    .card{style:"width: 30rem;"}
      = image_tag @pet.image.url(:medium), class: "card-img-top"

      .card-body
        .card-title
          %h5= @pet.name
        .card-textof
          %p
            %b Price:
            = @pet.price
          %p
            %b Tipo:
            = @pet.tipo
          %p
            %b Sexo:
            = @pet.sexo
          %p
            %b Observaciones:
            = @pet.observaciones
        = link_to "Regresar", root_path, class: "btn btn-primary btn-lg active"
        - if @pet.states.count > 0
          = link_to "Ver Estados", revisiones_path(id: @pet.id), class: "btn btn-primary btn-lg active"

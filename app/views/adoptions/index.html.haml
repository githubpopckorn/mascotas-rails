%h1 Mascotas adoptadas

.row
  - @adoptions.each do |adoption|

    .col-sm-12.col-md-6
      .card{style:"width: 30rem;"}
        = image_tag adoption.pet.image.url(:medium), class: "card-img-top"

        .card-body
          .card-title
            %h5= adoption.address
          .card-textof
            %p
              %b Latitud:
              = adoption.latitude
            %p
              %b Longitud:
              = adoption.longitude


            = link_to "Ver", adoption_path(id: adoption.id), class: "btn btn-primary btn-lg active"